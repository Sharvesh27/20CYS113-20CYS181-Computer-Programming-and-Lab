#include<stdio.h>
int main() {
    int rows,colums,space,k=1;
    printf("Enter no.of rows: ");
    scanf("%d",&rows);
    printf("Enter no.of colums: ");
    scanf("%d",&columns);
    
    for (space=0;space<rows-i;space++) {
        printf("\t");
    }
    for (i=0;i<rows;i++) {
        for (j=0;j<rows;j++) {
            if (j=0) {
                printf("%d",k);